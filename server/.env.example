PORT=4000

# PostgreSQL connection
# Format: postgres://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgres://postgres:postgres@localhost:5432/g08_dispute

# CORS — origin allowed to call the API (Vite dev server)
CLIENT_ORIGIN=http://localhost:5173

# Local upload directory for evidence files (relative to server/)
UPLOAD_DIR=uploads
